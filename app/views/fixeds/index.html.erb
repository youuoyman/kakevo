<div class="body">

<div class="title">
 <div class="main-title"><%= link_to "kakevo", root_path %></div>
</div>
<div class="fixed-index"><%= "固定費科目一覧" %>
</div>
<div class="fixed-new"><%= link_to "固定費科目の新規登録", new_fixed_path %>
</div>
 
<% if @fixeds.present? %>
<table>
	<thead>
		<tr>
			<th>科目名</th> 
			<th>備考</th>
      <th>操作</th>
		</tr>
	</thead>
	<tbody>
		<% @fixeds.each do |fixed| %>
			<tr>
				<td><%= fixed.name %></td>
				<td><%= fixed.description %></td>
        <td>
				<%= link_to "編集", edit_fixed_path(fixed.id) %>
        <%= link_to "削除", fixed_path(fixed.id), method: :delete %>
				</td>
			</tr>
		<% end %>
	</tbody>
</table>

<% else %>
	<p>登録されている収入科目がありません。</p>
<% end %>
</div>