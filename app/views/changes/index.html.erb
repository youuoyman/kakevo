<div class="body">

<div class="title">
 <div class="main-title"><%= link_to "kakevo", root_path %></div>
</div>

<div class="change-index"><%= "変動費科目一覧" %>
</div>
<div class="change-new"><%= link_to "変動費科目の新規登録", new_change_path %>
</div>
<% if @changes.present? %>
<table>
	<thead>
		<tr>
			<th>科目名</th>
			<th>備考</th>
      <th>操作</th>
		</tr>
	</thead>
	<tbody>
		<% @changes.each do |change| %>
			<tr>
				<td><%= change.name %></td>
				<td><%= change.description %></td>
        <td>
				<%= link_to "編集", edit_change_path(change.id) %>
        <%= link_to "削除", change_path(change.id), method: :delete %>
				</td>
			</tr>
		<% end %>
	</tbody>
</table>

<% else %>
	<p>登録されている収入科目がありません。</p>
<% end %>
</div>